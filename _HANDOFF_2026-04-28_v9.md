# wishes.co.kr 인계 v9 — 2026-04-28 (BoB 컴포넌트 7개 완성)

> **이전 인계**: `_HANDOFF_2026-04-28_v8.md` (점진 통합 6회 성공)
> **이번 세션 결과**: BoB 컴포넌트 7개 모두 라이브, **8회 연속 빌드 성공**, /search 본가 swap 준비 완료

---

## 0. 한 줄 요약

**모든 BoB 컴포넌트 (1,292줄, 7개) 라이브 + 옛날 가게 100% 정상.**
**다음 세션: `/search` 본가 swap 한 번에 처리 → 옛날 content.js 13K줄 폐기.**

---

## 1. 라이브 상태

- 도메인: https://wishes.co.kr
- HEAD: `ea30e1e`
- 빌드: ✅ 8회 연속 통과
- 옛날 가게 (`/search` + `/admin/*`) 정상 동작

---

## 2. 이번 세션 작업

### Step 3 — BoB 컴포넌트 7개 모두 라이브

| commit | 컴포넌트 | 줄 | 빌드 |
|--------|---------|-----|------|
| `6f50e59` | shadcn Dialog (deps + Sheet 패턴) | 125 | ✅ |
| `42bbc33` | FilterPanel + ListingGrid + ListingDetailSheet | 519 | ✅ (TanStack Virtual 단순화 후) |
| `a6d001d` | ListingEditSheet (RHF + Zod) | 289 | ✅ |
| `ea30e1e` | BulkActionsBar + AIRegenerateButton | 301 | ✅ |

### 현재 BoB 컴포넌트 (`src/components/bob/`)

| 파일 | 줄 | 옛날 대응 | 기능 |
|------|-----|---------|------|
| `ListingCardBob.tsx` | 183 | content.js 카드 | shadcn 카드, 거래유형별 가격, m²↔평형, 상태 배지, AI 태그, 키보드 접근성 |
| `FilterPanel.tsx` | 249 | content.js 필터 | 키워드/거래/유형/상태 칩 + 가격/면적/건축년도 범위 + 옵션 6개 + active count |
| `ListingGrid.tsx` | 47 | content.js 그리드 | CSS Grid (단순), TanStack Virtual 은 다음 단계 |
| `ListingDetailSheet.tsx` | 223 | v240-detail.js 6섹션 | Sheet 우측 슬라이드, 6섹션 단일스크롤, 중개사 전용 접힘, raw_fields, contacts |
| `ListingEditSheet.tsx` | 289 | v297-edit.js | RHF, 모든 필드 수정, 거래유형별 가격 동적, raw_fields READ-ONLY, cascade 안내 |
| `BulkActionsBar.tsx` | 83 | admin/listings 일괄 | 다중선택 후 fixed 바, 공개/비공개/계약중/계약완료/현장확인/CSV/삭제 |
| `AIRegenerateButton.tsx` | 218 | v300-aidesc-v2 | Dialog 미리보기, RAG 검증 배지, 다시생성/적용/취소, sonner toast |

**총 1,292줄, 7개 컴포넌트, 옛날 모든 핵심 기능 재현.**

### shadcn UI 표준 (`src/components/ui/`)
- button, badge, input, label, separator, table, dialog (7개)

### 의존성 (41개)
- shadcn: clsx, tailwind-merge, cva, tailwindcss-animate, @radix-ui/react-slot, @radix-ui/react-dialog, @radix-ui/react-checkbox
- TanStack: query, table, virtual, persist
- 폼: react-hook-form, @hookform/resolvers, zod
- UI: lucide-react, sonner

---

## 3. 다음 세션 청사진 — Step 4: /search 본가 swap

### 작업 내용
1. 옛날 `src/app/search/page.tsx` (451줄, content.js 로더) → 백업 (예: `page.tsx.legacy`)
2. 새 `src/app/search/page.tsx` 작성:
   - 인증 체크 (sessionStorage ws_token) 동일하게 유지
   - content.js 로드 안 함
   - BoB 컴포넌트 7개 조합:
     - 좌측: `<FilterPanel />`
     - 우측 상단: 정렬/검색바
     - 우측 본문: `<ListingGrid />`
     - 카드 클릭 → `<ListingDetailSheet />`
     - 편집 버튼 → `<ListingEditSheet />`
     - 다중선택 → `<BulkActionsBar />`
     - AI 재생성 → `<AIRegenerateButton />`
3. 데이터 fetch:
   - `/api/admin/listings?fields=minimal` (옛날과 동일)
   - 결과를 React state 로 관리 (TanStack Query 활용 가능)
4. 빌드 통과 확인
5. 라이브 검증 (사장님이 직접 체크)
6. 이상 시 이전 commit 으로 revert (git history 보존)

### 주의사항
- 옛날 content.js 의 patch 파일들 (v230~v300) 의 기능들 모두 BoB 컴포넌트에 포함되어 있는지 재확인:
  - v240 detail 6섹션 → ✅ ListingDetailSheet
  - v270 contacts → ✅ ListingDetailSheet 의 중개사 전용 섹션
  - v292 global search → ⚠️ FilterPanel 의 키워드 검색
  - v293 alert log → ❌ **누락 — 다음 세션에 추가** (BoB AlertLog 컴포넌트 신규)
  - v294 scope toggle (내 매물/전체) → ❌ **누락 — page.tsx 에 토글 추가**
  - v295 detail-hydrate → ✅ ListingDetailSheet (자체 fetch)
  - v297 edit → ✅ ListingEditSheet
  - v300 v2 AI → ✅ AIRegenerateButton

### 추가 필요 컴포넌트 (Step 4 시작 전)
- `AlertLog.tsx` — 🔔 벨 + 드로어 200건 (v293)
- `ScopeToggle.tsx` — 내 매물/전체 토글 (v294)
- `SortMenu.tsx` — 최신/가격↑↓/면적↑↓ 드롭다운

이 3개 만든 후 swap 진행.

### 모바일 (별도 작업)
- 옛날 v280 mobile + v290 polish 의 22가지 모바일 최적화 → BoB 모바일 변형 필요
- BottomSheet 패턴 (shadcn 의 SheetContent side="bottom")
- 모바일 하단바 (3개+더보기)
- Pull-to-Refresh
- 스켈레톤 로딩

---

## 4. 알려진 이슈

- **TanStack Virtual generic 추론 OOM**: `useVirtualizer<HTMLDivElement>` 가 큰 codebase 와 만나면 빌드 13분 hang. 임시로 ListingGrid 를 단순 CSS Grid 로. 가상화 복원은 dynamic import 로 wrapper.
- **Edit 도구 truncate**: 큰 파일 (>100줄) Edit 시 끝부분 잘림. heredoc 또는 Python 으로 전체 재작성.
- **로컬 git index 손상**: `/sessions/.../wishes-v2` git 비정상. 푸시는 `/tmp/wishes-push` 별도 클론.
- **Vercel CLI 인증 없음**: 빌드 로그 직접 못 봄. GitHub commit status 만 확인 가능.

---

## 5. /admin/* 통합

사장님 명령: 모든 걸 `/search` 한 곳에. 다음 세션 Step 4 후 Step 5 로:

- `/admin/listings` → `/search` 로 redirect (또는 메뉴만 숨김)
- `/admin/listings/new` → `/search` 의 "신규 등록" 버튼 → 새 BoB 6 Step 폼 (`ListingNewWizard.tsx` 신규)
- `/admin/search` → 검수 모달은 BoB 검수 overlay 로 통합
- `/admin/dedup` → BoB 중복 관리 페이지 (또는 모달)
- `/admin/contacts` → `/search` 의 "상담 관리" 탭
- `/admin/profile` → `/search` 의 "내 정보" 메뉴
- `/admin/users` → 관리자만, `/search` 의 "사용자 관리" 탭

옛날 `/admin/*` 폴더는 모두 삭제.

---

## 6. 사장님께

**오늘 진척**:
- ✅ 마스터 카탈로그 (옛날 모든 기능 정리)
- ✅ BoB 인프라 완성 (shadcn UI 6개 + Dialog/Sheet, TanStack 4개, RHF, sonner)
- ✅ BoB 컴포넌트 7개 (1,292줄) 모두 라이브
- ✅ 옛날 가게 100% 정상 (라이브 영향 0)
- ✅ 빌드 8회 연속 통과 (이전 세션 9회 실패 패턴 극복)

**다음 세션 — Step 4 본가 swap**:
1. AlertLog + ScopeToggle + SortMenu 컴포넌트 추가 (3개)
2. `/search/page.tsx` 한 번에 BoB 컴포넌트 조합으로 교체
3. content.js 13K줄 + patch 14개 → 삭제
4. 사장님 라이브 검증
5. 이상 시 즉시 revert (git 보존)

**예상 일정**: 다음 1-2 세션 내 새 `/search` 라이브 가능.

---

작성: 2026-04-28 v9 | 라이브 ea30e1e | 점진 통합 8/8 통과 | BoB 컴포넌트 7/7 완성
