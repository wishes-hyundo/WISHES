# WISHES 다음 세션 인계 (2026-04-29 v16)

이전 인계 (v15) 의 모든 영구 규칙·인프라·자동화 우선 원칙 그대로 유지.
이번 세션에서 새로 추가/변경된 것만 정리.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 이번 세션(2026-04-29 후반) 결과 — 라이브 적용
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### /search 카드/모달 표시 patches (vanilla JS, content.js 직접 수정 X)
- **v324 + v326**: 출처 뱃지 — 공실=파랑G(#1976D2) / 온하우스=빨강O(#E53935).
  주소 라인 앞 1곳만 노출 (mini 뱃지는 v326 cleanup).
- **v325**: 매물번호 강조 뱃지 — `[매물 {id}]` (#2D5A27 짙은녹색) 카드 .ws-listing-tags 첫 자식 고정.
  클릭 시 클립보드 복사. 우측 작은 매물번호 옆에 mini 뱃지는 cleanup.
- **v327b**: 카드 부 라인 = 도로명주소. 시/구 prefix 자동 제거 ("서울 관악구 신림로11길 123-14" → "신림로11길 123-14"). 옅은 회색 #6b7280.
- **v328b**: 메인 라인 형식 통일 — 매물 61443 기준
  - `[지번주소] [건물명] [N층] [동] [호]`
  - 슬래시(/Nt) 없음 / 분모 없음 / "호" 글자 없음 / 쉼표 없음
  - 가동/나동/A동/B동 등 building_dong 자동 부착 ("동" 끝나면 그대로)
  - "(괄호 건물명)" 자동 제거
- **v329**: 공실 라벨 정확화 — `available_date` 기준 분기
  - `(공실)` → 공실 (#2D5A27 녹색)
  - `(비공실)` → 거주중 (#6B7280 회색)
  - `협의*` → 협의입주 (#E65100 주황)
  - `YYYY-MM-DD` → "YY.MM 입주" (#0369A1 파랑)
  - 빈/null → 라벨 hide
- **v330**: 룸 라벨 — listing.rooms 변환
  - 1=원룸 / 1.5=1.5룸 / 2=투룸 / 3=쓰리룸 / 4+=쓰리룸+
- **v331**: onhouse 매물 모달 검정화면 → placeholder ("🏠 사진 준비중" + 원본 링크)

### 백엔드 수정
- **/api/admin/listings minimal selectFields**: `building_info`, `building_dong`, `building_ho`, `rooms` 추가
  - building_info 슬림화 (도로명주소/지번주소 두 키만 남김 — 페이로드 size 절감)
  - cacheKey v9 → v11 (60초 stale 강제 무효화)
- **/api/img-proxy**: ALLOWED_HOSTS 확장
  - + `www.onhouse.com`, `onhouse.com`, `image.onhouse.com`, `img.onhouse.com`, `cdn.onhouse.com`
  - + `gongsilclub.com`, `www.gongsilclub.com`, `image.gongsilclub.com`

### 신규 endpoints
- **/api/admin/enrich-roadname** (POST + GET):
  - mode='onhouse-keyword': building_name 만 있는 onhouse 매물 → kakao keyword search → 풀 지번 + 도로명 + 좌표
  - mode='roadname': address 풀주소 매물 → kakao address search → 도로명주소
  - cursor pagination (lastId), gu/dong filter, batchSize ≤ 100
- **/api/admin/enrich-onhouse-detail** (POST + GET):
  - ONHOUSE_USERNAME / ONHOUSE_PASSWORD env 자동 로그인 → ci_session 획득
  - source_url fetch + HTML regex → building_dong / building_ho / address_detail 추출
  - **사장님이 Vercel UI 에 ID/PW 등록 후 자동 시작** (현재는 graceful skip)
  - 인증: ENRICH_TOKEN (수동) + Vercel cron (Authorization Bearer CRON_SECRET / user-agent vercel-cron)

### 자동화 (Vercel cron)
- **매 30분**: `/api/admin/enrich-roadname?mode=roadname&batchSize=80` — 16,500건 도로명 약 4일 자동 백필
- **매일 03:00 KST**: `/api/admin/enrich-onhouse-detail?batchSize=20` — 신규 onhouse 매물 호수 자동 enrich (env 등록 후)

### DB 정리 (이번 세션 수동 백필)
- onhouse 매물 28건 SQL 정규화 (title 풀주소 → address/address_detail/building_ho)
- onhouse 25건 카카오 keyword enrich (맥스텔/해주타워/세진빌/엠코빌 등 → 풀주소 + 도로명 + 좌표)
- 모든 매물 290건 도로명주소 백필 (관악구 위주)
- 13건 카카오 no-match (불루빌/금강원룸/IRIS빌딩 등 작은 빌라 — 카카오 미등록) → ID/PW 등록 후 onhouse-detail 로 자동 enrich

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 사장님 작업 (1번만)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vercel UI 에 환경변수 등록 — onhouse 호수 자동 enrich 활성화:
1. https://vercel.com/wishes-hyundo/wishes/settings/environment-variables
2. ONHOUSE_USERNAME 추가 (사장님 onhouse ID)
3. ONHOUSE_PASSWORD 추가 (비밀번호)
4. 모든 환경 (Production/Preview/Development) 체크
5. Save → Deployments → 최신 → Redeploy
6. 등록 완료되면 매일 03:00 KST cron 자동 실행

(선택) CRON_SECRET 등록 시 더 강한 cron 인증.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 다음 세션 즉시 액션
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. sandbox 셋업: /tmp/work-2026-04-30/wishes-clone 에 fresh clone
2. live audit 실행 (8/8 PASS 기대)
3. 도로명 백필 진행 상황 확인:
   ```sql
   SELECT COUNT(*) FILTER (WHERE building_info->>'도로명주소' IS NOT NULL AND building_info->>'도로명주소' != '') AS done,
          COUNT(*) AS total
   FROM listings;
   ```
   기대: 매 30분 cron 으로 점진 증가. 4일 후 거의 100%.
4. ID/PW 등록되었으면 onhouse 호수 enrich 결과 확인:
   ```sql
   SELECT COUNT(*) FROM listings WHERE source_site='onhouse' AND building_ho IS NOT NULL;
   ```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 미정리 (다음 세션 cleanup 후보)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- ENRICH_TOKEN (`wishes-enrich-2026-04-29-onetime-Y3b8H2mK`) git history 에 박혀있음.
  → CRON_SECRET env 로 전환 후 token 제거 commit 권장.
  현재는 endpoint 가 cron + token 둘 다 받음.
- enrich-onhouse-detail 의 호수 추출 regex 3개 패턴은 추정 — 실제 onhouse HTML 구조와 다를 수 있음.
  사장님 ID/PW 등록 후 첫 호출 시 body.debug=true 로 HTML sample 확인하고 정규식 조정 필요할 수 있음.
- 카카오 no-match 13건 (불루빌, 금강원룸, IRIS빌딩 등) — onhouse-detail 로 처리되면 해결.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
이번 세션 마지막 commit: 9845d7d
이전 commit: d84682d (enrich-onhouse-detail) → ad739a0 (v328b/v330) → d2c23d5 (v327b/v328/v329) → 36f396c (list API building_info) → 840d8ec (enrich-roadname) → 3957751 (v326) → ecee7bd (v325) → de7db41 (v324b) → e9bdd17 (v324)
종료: 2026-04-29 KST
