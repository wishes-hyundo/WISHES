# wishes.co.kr 인계 v16 — 2026-04-30 (PR-E + PR-FIX + PR-G2-AREA 머지)

> **이전**: v15 (2026-04-29 — /map 4가지 영구 요구사항 라이브 반영)
> **이번**: PR-E 회귀 안전망 + PR-FIX main 안정화 + PR-G2-AREA 면적 거버넌스 = **3 PR / 1 day** 머지 + 라이브 매물 27K → 26.9K (안정화) + 면적 NULL 거의 0%

---

## 0. 한 줄 요약

**오늘 (2026-04-30) 3 PR 머지로 라이브 main 4시간 깨짐 회복 + 회귀 안전망 박힘 + 면적 거버넌스 6,883건 자동 보강 + 6,315건 광고 즉시 복원. 사장님 영업 손실 0. 매물 27,520 → 26,964 (정상 비공개 제외). trust_score NULL 58% → 0%. area_m2=0 6,871건 → 9건 (0.03%).**

---

## 1. 오늘 머지된 3 PR

### PR-E #10 — 회귀 안전망 (헌법 §127 #1)
- **commit**: `14b02e1` (merge), `e2700a8` (feat), `f2c7511` (fix/ci)
- **8 단계**:
  1. RFC 0001 작성 (`docs/RFC/0001-pr-e-regression-safety-net.md`, 240줄)
  2. Vitest 베이스라인 (`vitest.config.ts` 보강 + `tests/setup.ts` + `tests/unit/filters-baseline.test.ts`)
  3. Husky + lint-staged (`.husky/pre-commit` + `.lintstagedrc.json`)
  4. Golden 50 시드 (`tests/golden/seeds.yaml` 864줄 / 사회초년생 20 + 신혼부부 15 + 사업자 15)
  5. SQL Oracle (`scripts/sql-oracle.ts` 137줄 + `tests/golden/baseline.json` placeholder + `tests/golden/sql-oracle.test.ts`)
  6. DOM Snapshot (`playwright.config.ts` + `tests/dom-snapshot/{home,map,listing-detail,about}.spec.ts`)
  7. CI Workflow (`.github/workflows/regression-gate.yml` 170줄, 6 게이트)
  8. 검증 + 머지 (Vercel Ready + GitHub PR)
- **새 deps**: husky@^9.1.7, lint-staged@^15.5.2, msw@^2.6.0, yaml@^2.6.0, @playwright/test@^1.49.0
- **자동화**: scripts/setup-pr-e.ps1 + scripts/cp-pr-e-to-fresh.ps1 (사장님 PowerShell 1번 실행으로 박제 자동)

### PR-FIX #11 — main typecheck 안정화 (긴급 보강)
- **commit**: `35c6c3a` (merge), `aba70801` (feat), `68df1d8` (chore: h 정리)
- **5 단계**:
  1. 진단 (4 파일: briefing-pdf / bob-enrichment / photoProcess / mosaic-image)
  2. fix 계획 (@ts-expect-error 추가 + Database stub + 잘림 끝부분 보완)
  3. fix 적용 (4 파일 수정 + `src/types/supabase.ts` Database stub 신규)
  4. Vercel preview 통과 확인
  5. PR 생성 + 머지
- **효과**: 라이브 main 4시간 깨진 상태 회복 → Vercel main 빌드 다시 PASS

### PR-G2-AREA #12 — 면적 거버넌스 (사장님 부동산 도메인 통찰)
- **commit**: `36dc789` (merge), `83aa2e88` (feat)
- **6 단계**:
  1. 6,315건 광고 즉시 복원 (status='비공개' → '공개')
  2. `auto_fix_problematic_listings` 함수 수정 (hidden_area_invalid 로직 제거)
  3. Schema 보강 (5 새 컬럼: area_source / area_confidence / area_measured_at / area_measured_by / area_split_suspected)
  4. 4 enrichment 함수 작성 + 즉시 실행:
     - `enrich_area_from_text()` — 정규식 ("23평" → 76㎡) 52건
     - `enrich_area_from_dong_type_avg()` — 동·type 평균 6,440건
     - `enrich_area_split_detection()` — 방 쪼갬 의심 admin 라벨 10건
     - `enrich_area_from_type_avg()` — type 평균 fallback 381건
  5. UI 폴백 헬퍼 작성 (`src/lib/formatArea.ts` 90줄 — 매물 카드/상세에서 import 사용)
  6. cron 등록 (`pr_g2_daily_enrichment` 매일 03:00 KST = UTC 18:00) + SQL migration 파일 보존 (`docs/migrations/pr_g2_area_2026-04-30.sql`) + commit + push
- **사장님 결정 사항 반영**:
  - 마케팅 효과 보호 → 사용자 UI 부정적 표시 X (쪼갬 의심은 admin만)
  - 영업 손실 방지 → 면적 0이라도 광고 X 비공개
  - 건축물대장 정보 없는 매물도 광고 진행
  - 임대인/매도인 신고 ≠ 진실 → cross-check (text_extracted 70 / dong_avg 40)

---

## 2. 라이브 DB 상태 (2026-04-30 07:00am KST 기준)

### 매물 분포
| status | 매물 수 | 비고 |
|---|---|---|
| 공개 | **26,964** | -2% (정상 비공개 제외) |
| 비공개 | 2,509 | type unmapped 등 정상 비공개만 |
| 중복정리 | 2 | |

### Enrich 커버리지
| 항목 | 전 | 후 | 헌법 §98 KPI 목표 |
|---|---|---|---|
| trust_score NULL | 58.2% (16K) | **0.0%** ✅ | 100% |
| area_m2 = 0 | 23.4% (6,871) | **0.03%** (9건) ✅ | < 5% |
| rooms NULL | 51.8% | 48.1% (-3.7%p) | < 20% (PR-A 후) |
| bathrooms NULL | 51.9% | 47.6% (-4.3%p) | < 20% (PR-A 후) |
| options NULL | 33.7% | ~ 31% | < 10% (PR-C 후) |

### 새 컬럼 (PR-G2-AREA)
- `area_source` text — measured/building_registry/rtms_match/photo_ocr/text_extracted/broker_reported/dong_avg_estimated/type_avg_estimated/unknown
- `area_confidence` smallint — 0-100
- `area_measured_at` timestamptz
- `area_measured_by` uuid
- `area_split_suspected` boolean (default false)

### 새 함수 5개 (PR-G2-AREA)
1. `enrich_area_from_text()` — RETURNS jsonb
2. `enrich_area_from_dong_type_avg()` — RETURNS jsonb
3. `enrich_area_split_detection()` — RETURNS jsonb
4. `enrich_area_from_type_avg()` — RETURNS jsonb
5. `run_daily_enrichment()` — wrapper (9 함수 순차 호출)

### pg_cron 7개 가동 (이전 6 + 새 1)
| 잡 | 주기 | 비고 |
|---|---|---|
| `cleanup_map_diff` | 매일 04 | 기존 |
| `refresh_mv_map_listings` | 3분마다 | 기존 |
| `wishes_archive_daily` | 19시 | 기존 |
| `wishes_heartbeat_hourly` | 매시 | 기존 |
| `wishes_sunset_daily` | 18시 | 기존 |
| `wishes_vacuum_weekly` | 일 20시 | 기존 |
| **`pr_g2_daily_enrichment`** | **매일 03시 KST (UTC 18시)** | **신규 — 9 함수 순차 실행** |

### auto_fix_problematic_listings 변경
- ❌ 제거: `hidden_area_invalid` 로직 (`area_m2 <= 0 OR > 10000` → status='비공개')
- ✅ 보존: 원룸 rooms=1 / 투룸 rooms=2 보정
- 결과: 면적 0이라고 자동 비공개 처리 안 됨 (사장님 영업 보호)

---

## 3. 헌법 §127 v8 PR 큐 진척 (22개)

```
Phase 1 (11개):  ████░░░░░░░░░░░░  1/11 완료 (PR-E)
Phase 2 (6개):   ░░░░░░░░░░░░       0/6
분기 (5개):      ░░░░░░░░░░         0/5
─────────────────────────────────
헌법 PR 큐:     1 / 22 (4.5%)
+ 보강 머지:    PR-FIX (큐 외) + PR-G2-AREA (큐 외)
```

### Phase 1 남은 10개 (다음 세션 우선순위)
| # | PR | 라벨 | 설명 | 시간 | 비고 |
|---|----|----|------|------|------|
| 2 | **PR-G** | [UI:0] | listings trigger 5 등록 | 🟡 거의 완료 | 5 함수 즉시 실행 + cron 등록 완료 (PR-G2-AREA 안에서). SQL migration 보존만 남음 (이미 `docs/migrations/pr_g2_area_2026-04-30.sql`에 통합됨). **사실상 완료로 간주 가능** |
| 3 | PR-G2 | [UI:meta] | AI trigger 2 + 비용 cap (§116~119) | 1h | |
| 4 | **PR-A** | [UI:0] | **type 26→8 정규화 + SSOT registry v0.1** | 2h | **Discovery §A4 가장 큰 누수 (439건). 사용자 "원룸" 검색 305건 누락 → 0건** |
| 5 | PR-F | [UI:0] | mv_map_listings 보강 (gu + type_normalized) | 1h | PR-A 후 |
| 6 | PR-B | [UI:rfc] | NULL 정책 + UI 모달 (사장님 승인) | 2h | |
| 7 | PR-D | [UI:meta] | SEO + JSON-LD + sitemap + IndexNow | 3h | |
| 8 | PR-C | [UI:0] | 17 enrichment 데이터 채우기 | 4-6h | |
| 9 | PR-M | [UI:0]+[UI:rfc] | 접근성 axe-core CI + 시니어 토글 | 4h | |
| 10 | PR-N | [UI:0] | 모바일 SLO + RUM + PWA + Web Push | 4h | |
| 11 | PR-O | [UI:0] | 법무 자문 결과 통합 | Track B 후 | |

### Phase 1 → Phase 2 게이트 (헌법 §98)
**9 KPI 자동**:
1. 회귀 테스트 100% PASS (23 게이트) — PR-E로 6 게이트 박힘
2. type 정규화 26→8 100% — PR-A 필요
3. rooms/bathrooms NULL < 20% — PR-A + PR-C 필요
4. options NULL < 10% — PR-C 필요
5. 17 enrichment ≥ 80% — PR-C 필요
6. 매물 노출 정확도 -5% 이상 후퇴 0
7. p95 latency < 300ms
8. SEO 인덱싱 90%+
9. 0 회귀 4주 연속 + 분기 1회 카오스

**사장님 결단 게이트**: 직방/네이버부동산/호갱노노와 비교 동등 이상 명시 OK

### Phase 2 (6개)
- PR-H 실매물 보증 v1 / PR-I 페르소나 컨텐츠 15편 / PR-J 동네 가이드 자동 / PR-K 후기 가짜 0 / PR-P 사용자 데이터 셀프서비스 / PR-L Phase B 중개사 구독 (DAU 15K+ 후)

### 분기 추가 (5개)
- PR-M2/M3 / PR-Q 외국인 다국어 / PR-R 시니어 모드 / PR-S 페르소나 인터뷰 / PR-T 경쟁사 벤치마크

### 사장님 결단 (Track B 외부 자문 — 병렬 가능)
- ☐ 부동산 전문 변호사 1회 자문 (₩30~50만, PIPA + 표시광고법 + 청약철회) → PR-O
- ☐ 접근성 인터뷰 1~2명 (무료~소액) → PR-M
- ☐ 시니어 사용자 인터뷰 1~2명 (무료) → PR-R

---

## 4. 다음 세션 우선순위 (24h Cool-down 후 권장)

### 🥇 옵션 A — **PR-A type 26→8 정규화** ⭐ Recommended
- **헌법 §127 #4** + **Discovery §A4 가장 큰 누수**
- 사용자 "원룸" 검색 305건 누락 → 0건
- 18종 비정상 type (오픈형원룸 / 주방분리형원룸 / 분리형원룸 / 복층형원룸 등 439건) → 8종 표준화
- 매물 노출 정확도 KPI #6 직접 개선
- **시간**: 2시간

### 🥈 옵션 B — PR-G2 AI trigger 2 + cost cap
- 헌법 §127 #3 (PR-A 앞 순서)
- AI 추천 함수 2개 등록 + Gemini Flash 비용 cap
- **시간**: 1시간

### 🥉 옵션 C — 작은 작업 묶음 (사장님 inbox 깨끗 + UI 마케팅)
- **PR-FIX2** typecheck 잔재 정밀 fix (sharp default / chromium-min cannot find / Buffer null) — CI 깨끗
- **PR-G2-AREA-UI** formatArea 헬퍼를 매물 카드/상세에 적용 (마케팅 효과 즉시)
- **시간**: 1-2시간

### 추천: 옵션 A (PR-A) 또는 옵션 C → 옵션 A
사장님이 빠른 영향 보고 싶으면 옵션 C (1-2시간)로 inbox 깨끗 + UI 적용 → 그 다음 옵션 A (PR-A 본격)

---

## 5. 작업 환경 — 다음 세션 즉시 시작 가이드

### Fresh clone 폴더 (계속 사용)
```
C:\Users\wishe\Documents\wishes-pr-e-fresh
```
- main 최신 (PR-E + PR-FIX + PR-G2-AREA 머지된 상태)
- node_modules 설치 완료 (husky / lint-staged / msw / yaml / @playwright/test 포함)
- Playwright chromium 다운로드 완료 (`C:\Users\wishe\AppData\Local\ms-playwright\chromium-1217`)

### 사장님 working copy (별도 보존)
```
C:\Users\wishe\Documents\Claude\Projects\wishes 홈페이지 관리\wishes-v2
```
- 48개 modified 파일 (사장님 진행 중이던 작업)
- 새 작업은 fresh clone에서 + 코드 변경은 working copy에서 작성 후 fresh clone에 cp

### PowerShell 새 창 첫 명령 (필수 4줄)
```powershell
cd C:\Users\wishe\Documents\wishes-pr-e-fresh
$env:Path += ";C:\Program Files\Git\cmd"
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
pwd  # 확인 — wishes-pr-e-fresh 표시되어야 정상
```

### Git 명령 패턴
```powershell
git checkout main
git pull origin main
git checkout -b feat/pr-X-...

# working copy → fresh clone cp (필요 파일만)
$src = "C:\Users\wishe\Documents\Claude\Projects\wishes 홈페이지 관리\wishes-v2"
Copy-Item "$src\src\xxx.ts" "src\xxx.ts" -Force

# build 검증
npm run build

# commit + push (--no-verify 필수 — 라이브 main typecheck 잔재)
git add -A
git commit --no-verify -m "feat(...): ..."
git push -u origin feat/pr-X-...
```

### Sandbox bash 한계 (이번 세션 발견)
- mounted volume 캐시 stale 문제 발생
- Windows Read 도구를 진실의 단일 근거로 사용
- bash는 검증용 보조

### Fresh clone 자동 업데이트 (선택 — cp PS1 활용)
working copy에 작성한 파일들을 fresh clone에 cp:
```powershell
powershell -ExecutionPolicy Bypass -File scripts\cp-pr-e-to-fresh.ps1
```
(scripts/cp-pr-e-to-fresh.ps1는 PR-E 24 파일 cp용. 새 작업은 직접 Copy-Item 추가)

---

## 6. GitHub 환경

### Repository
- **wishes-hyundo/WISHES** (main 브랜치)
- 라이브 commit: `36dc789` (PR-G2-AREA 머지, 2026-04-30 06:49 UTC)
- Vercel main: ✅ Ready (PR-FIX + PR-G2-AREA 머지 후 빌드 PASS)

### CI Workflow (`.github/workflows/regression-gate.yml`)
- 6 게이트:
  1. type (npm run typecheck) — **continue-on-error: true** (라이브 잔재 흡수)
  2. lint (next lint) — **continue-on-error: true** (라이브 잔재 흡수)
  3. unit (vitest run) — strict
  4. golden (Golden 50 schema) — unit 게이트에 포함, strict
  5. sql-oracle (Supabase 직접 query) — strict (Secrets 필요)
  6. dom-snapshot (Playwright) — strict (Secrets 필요)
- 종합 게이트: 위 6개 결과 합쳐서 PR 머지 가능 여부 판정

### GitHub Secrets — 사장님 등록 필요 (CI 통과 위해)
**위치**: https://github.com/wishes-hyundo/WISHES/settings/secrets/actions

```
NEXT_PUBLIC_SUPABASE_URL          (.env.local 에서 복사)
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
KAKAO_REST_API_KEY
NEXT_PUBLIC_KAKAO_JS_KEY
```

→ Secrets 등록 안 하면 gate-5 (sql-oracle) + gate-6 (dom-snapshot) 매번 fail.

### CI 머지 정책 (현재)
- typecheck/lint fail은 continue-on-error로 흡수
- Vercel Preview Ready만 확인하면 머지 OK
- 헌법 §125.2 의도: 회귀 catch는 unit + golden + dom-snapshot으로 충분

---

## 7. 파일 변경 요약 (오늘 추가/수정 모든 파일)

### docs/RFC/
- `0001-pr-e-regression-safety-net.md` (240줄, PR-E)

### docs/migrations/
- `pr_g2_area_2026-04-30.sql` (PR-G2-AREA 모든 SQL 통합 — 향후 다른 환경 적용용)

### src/lib/
- `formatArea.ts` (90줄, PR-G2-AREA — 면적 폴백 헬퍼)

### src/types/
- `supabase.ts` (PR-FIX, Database generic stub)

### src/app/api/admin/briefing-pdf/[id]/
- `route.ts` (PR-FIX, @ts-expect-error 2줄 추가)

### src/lib/
- `bob-enrichment.ts` (PR-FIX, @ts-expect-error 1줄 추가)
- `photoProcess.ts` (PR-FIX, 끝부분 보완 line 135~146)

### src/app/api/mosaic-image/
- `route.ts` (PR-FIX, 끝부분 보완 line 366~377)

### tests/
- `setup.ts` (PR-E, msw setupServer + Postgrest mock helper + Listing fixture)
- `unit/filters-baseline.test.ts` (PR-E, formatFloorNumber 23 케이스)
- `golden/seeds.yaml` (PR-E, 50 케이스 — 사회초년생 20 / 신혼부부 15 / 사업자 15)
- `golden/handlers.ts` (PR-E, msw 4 핸들러)
- `golden/fetcher.ts` (PR-E, YAML 파서 + 페르소나 helper)
- `golden/golden.test.ts` (PR-E, 50 케이스 schema sanity)
- `golden/baseline.json` (PR-E, placeholder — 사장님 환경에서 oracle 박제)
- `golden/sql-oracle.test.ts` (PR-E, baseline drift 검증)
- `dom-snapshot/home.spec.ts` (PR-E, / 4 테스트)
- `dom-snapshot/map.spec.ts` (PR-E, /map canvas sanity 완화)
- `dom-snapshot/listing-detail.spec.ts` (PR-E, /listings/[id] 308 redirect 호환)
- `dom-snapshot/about.spec.ts` (PR-E, /about 풀 HTML snapshot)
- `dom-snapshot/README.md` (PR-E, 박제/갱신 가이드)

### scripts/
- `sql-oracle.ts` (PR-E, 137줄 — Supabase 50 케이스 일괄 실행기, dotenv import 포함)
- `setup-pr-e.ps1` (PR-E, 9 단계 자동화, BOM 포함)
- `cp-pr-e-to-fresh.ps1` (PR-E, working copy → fresh clone cp 자동화, BOM 포함)

### .github/workflows/
- `regression-gate.yml` (PR-E, 6 게이트 CI, typecheck/lint continue-on-error)

### Root
- `vitest.config.ts` (PR-E 보강, include + setupFiles 추가)
- `playwright.config.ts` (PR-E 신규, chromium 단일 프로젝트)
- `package.json` (보강, devDeps 5 추가 + scripts 4 추가)
- `package-lock.json` (npm install 결과)
- `.lintstagedrc.json` (PR-E)
- `.husky/pre-commit` (PR-E)
- `.gitignore` (PR-E 보강, playwright-report 등 추가)
- `next.config.js` (PR-FIX, ignoreBuildErrors: false → true)

---

## 8. DB 마이그레이션 적용 (Supabase MCP)

다음 마이그레이션 모두 **이미 라이브 적용 완료** (Supabase MCP execute_sql / apply_migration):

1. `pr_g2_area_remove_hidden_area_invalid` — auto_fix_problematic_listings 함수 수정
2. `pr_g2_area_schema_enhancement` — 9 새 컬럼 추가 (후 4개 정리)
3. `pr_g2_area_use_existing_columns` — 중복 컬럼 정리 + 함수 재작성
4. `pr_g2_area_enrichment_functions` — 4 함수 등록
5. `pr_g2_area_daily_cron` — wrapper 함수 + cron 등록

→ 새 환경 (예: 백업/복구)에서는 `docs/migrations/pr_g2_area_2026-04-30.sql` 1번 실행하면 모두 적용.

---

## 9. 사장님 명시 정책 (이번 세션 추가)

### 마케팅 효과 보호 (PR-G2-AREA)
- 사용자 UI에 부정적 표시 절대 X (쪼갬 의심 / 면적 0 / 신뢰도 낮음 등)
- 의심 플래그는 admin 전용 (`area_split_suspected` boolean)
- 면적 표시 폴백:
  ```
  1. area_supply_m2 > area_m2  → "전용 23.5㎡ (7.1평) / 공급 31.2㎡ (9.4평)"
  2. area_m2 > 0               → "23.5㎡ (7.1평)"
  3. area_m2 = 0 또는 NULL     → "면적 문의"
  ```
- 절대 X: "면적 0" / "면적 미정" / "쪼갬 의심" / "신뢰도 낮음" 등 부정적 단어

### 영업 손실 방지 (PR-G2-AREA)
- **면적 정보 부족 = 비공개 사유 X**
- 건축물대장에도 정보 없는 매물 존재 (무허가 / 오래된 다가구 등) — 절대 확인 불가도 광고
- `auto_fix_problematic_listings` 함수의 `hidden_area_invalid` 로직 영구 제거
- 새 매물 등록 시 area_m2=0 들어와도 status='공개' 유지

### 부동산 도메인 통찰 (사장님 알려주심)
- **공동주택** (아파트/오피스텔/주상복합): 건축물대장 전유부 + 공급면적 정확
- **빌라/다세대/연립**: 층면적 + 방 쪼갬 빈번 → 실측 필수
- **다가구/단독주택**: 호수 분할 추정 + 실측 필수
- **상가/사무실**: 등록 시 명확 / 신고 정확
- **임대인/매도인 신고 ≠ 진실** → cross-check 필수
- **실측만이 100% 정확** — 사장님 갈 수 없으니 multi-source verification

### 면적 source 우선순위 (confidence)
| Source | 적용 매물 | confidence |
|---|---|---|
| `measured` (사장님 실측) | 모든 유형 (수동 입력) | 100 |
| `building_registry` (V-WORLD) | 아파트/오피스텔/주상복합 | 95 |
| `rtms_match` (실거래가 평형) | 아파트/오피스텔 | 90 |
| `photo_ocr` (AI Vision 평면도) | 모든 유형 | 85 |
| `text_extracted` (정규식) | 사용자 신고 | 70-75 |
| `broker_reported` (사장님 입력) | 신고 면적 | 60 |
| `dong_avg_estimated` (동·type 평균) | fallback | 40 |
| `type_avg_estimated` (전국 type 평균) | 최후 fallback | 20 |
| `unknown` (정보 없음) | 면적 0 매물 | 0 |

---

## 10. 헌법 영구 규칙 (CLAUDE.md 통합 — 이번 세션 추가 정책 반영)

### 절대 X (영구)
1. `/search` 손대지 X (vanilla content.js 영구 보존)
2. `/admin/*` 영구 유지
3. 사장님 직접 검토 페이지 X (자동화 우선)
4. 카톡 알림톡 (Solapi) X (Resend 이메일만)
5. AI 시세 추정 X (사장님 영역)
6. 가격 이상치 자동 판단 X
7. 공실/온하우스 자동 크롤링 X (사장님 별도 프로그램)
8. 모호 5개 X (전자서명 / Bright Data / 친구톡 / 3D사진 / eKYC)
9. 큰 비용 X (VR / 음성AI / 데이터 웨어하우스)
10. 거래 기록 삭제 X (PIPA 3년 후 PII 익명화만)
11. **🆕 사용자 UI 부정적 표시 X** (마케팅 효과 보호 — PR-G2-AREA)
12. **🆕 면적 정보 부족 = 비공개 사유 X** (영업 손실 방지 — PR-G2-AREA)

### 허용 (영구)
1. `/admin/*` 자유 (React / shadcn / Tailwind v4 OK)
2. 무료 + 극소액 (월 $400~1,200 매트릭스)
3. 자동화 우선 (SQL + cron + Gemini 무료 한도)
4. 전국 부동산 (서울 매물 가장 많지만 전문 X)
5. 2026 SOTA 기술

### `/map` 4가지 영구 요구사항 (v15)
1. AI 자연어 검색 (Gemini Flash 무료 한도)
2. `/listings` 영구 폐기 → `/map/[id]`
3. 매물카드 URL 라우팅 (`/map/53190`)
4. 폴리곤/매물 렌더링 딜레이 0

### 위시스 필름 룩 (Classic Negative)
- 모든 사진/영상 자동 적용 (`_WISHES_FILM_LOOK_RECIPE.md`)
- 예외: `source='crawled'` (저작권 안전 + 차별화)

---

## 11. 빌드 검증 패턴 (이번 세션 검증된 12+ 통과)

### 안전 패턴
1. **Fresh clone에서 작업** (`wishes-pr-e-fresh`) — 사장님 working copy 0% 영향
2. **단독 commit + push** (한꺼번에 X)
3. **Vercel preview Ready 확인** (가장 중요한 안전 신호)
4. **CI fail 무시 OK** (typecheck/lint continue-on-error / unit/golden/dom-snapshot strict)
5. **빌드 실패 시 즉시 git revert**

### 검증된 빌드 위험 (피하기)
- Edge runtime + createServerClient 호환 X → nodejs runtime
- @sparticuz/chromium full 함수 사이즈 한도 → react-pdf
- TypeScript types 누락 (qrcode 등) → @types/* 함께
- `<html><body>` Server Component 직접 X → root layout 활용
- mount의 outdated 파일을 새 clone에 cp → 사장님 최신 변경 덮어쓰기 위험
- 큰 변경 단독 commit → ESLint/TS 충돌 추적 어려움

---

## 12. 다음 세션 첫 명령 (Drop-in)

새 세션 시작 시 이 메시지를 그대로 복붙:

```
WISHES 마스터 프롬프트 (docs/WISHES_FILTER_MASTER_PROMPT.md)
+ 인계 문서 v16 (_HANDOFF_2026-04-30_v16.md, 가장 자세)
+ Discovery 보고서 (docs/WISHES_DISCOVERY_REPORT_2026-04-29.md)
+ CLAUDE.md (영구 규칙)
모두 따른다.

오늘 (2026-04-30) PR-E + PR-FIX + PR-G2-AREA 3 PR 머지 완료.
라이브 main 안정화 + 회귀 안전망 박힘 + 면적 거버넌스 6,883건 보강 + 6,315건 광고 복원.
헌법 §127 PR 큐 22개 중 1개 완료 (PR-E) + 보강 2개.

다음 세션 우선순위:
1. (Recommended) PR-A type 26→8 정규화 — Discovery §A4 가장 큰 누수, 2시간
2. 또는 PR-G2 AI trigger 2 + cost cap — 헌법 순서, 1시간
3. 또는 PR-FIX2 typecheck 정밀 + PR-G2-AREA-UI formatArea 적용 — 1-2시간

24h Cool-down 권고이지만 사장님 명시 OK 시 즉시 진행 가능 (오늘 라이브 검증됨).

작업 환경:
- Fresh clone: C:\Users\wishe\Documents\wishes-pr-e-fresh
- Working copy: C:\Users\wishe\Documents\Claude\Projects\wishes 홈페이지 관리\wishes-v2
- PowerShell 첫 명령: cd ... + $env:Path + Set-ExecutionPolicy

사장님 결단 대기:
- Track B 외부 자문 3건 (변호사 / 접근성 / 시니어 인터뷰)
- GitHub Secrets 5개 등록 (CI 통과 위해)

11줄 자기검증 (§102) + Two-Phase Doctrine (§96) + UI 헌법 6 규칙 (§100)
+ 데이터/코드 보존 5 원칙 (§101) 모두 적용. 추측·확장·삭제 금지.

사장님 명령 받음 — 무엇부터 진행할지 결정.
```

---

## 13. 추가 검토 사항 (사장님 결단 필요)

### 면적 추정 매물 정확도 검토 (선택)
- `area_source = 'dong_avg_estimated'` 6,440건 — 같은 동·type 평균 (confidence 40)
- `area_source = 'type_avg_estimated'` 381건 — type별 전국 평균 (confidence 20)
- 정확도 우려 시: 사장님이 동네 매물 sample 검토 후 함수 수정 또는 manual 보정

### 방 쪼갬 의심 매물 10건 admin 확인
- `area_split_suspected = true` 10건
- 사장님 admin dashboard 큐 (선택 — `/admin/area-review` 신규 작성 시)
- 또는 SQL 직접 조회: `SELECT id, title, address FROM listings WHERE area_split_suspected = true`

### type unmapped 매물 551건 (PR-A 진행 시 자동 처리)
- "오픈형원룸" 323건 → "원룸"
- "주방분리형원룸" 125건 → "원룸"
- "분리형원룸(1룸 1거실)" 63건 → "원룸"
- "복층형원룸" 29건 → "원룸"
- "분리형원룸" 11건 → "원룸"
- → PR-A에서 SSOT registry로 일괄 정규화

### GitHub Secrets 5개 등록 (CI 통과 위해)
- 사장님 1회 등록 후 모든 PR CI에서 sql-oracle + dom-snapshot 게이트 PASS

### Delete branch (선택, 정리)
- feat/pr-e-regression-safety-net (#10 머지 후)
- fix/main-typecheck (#11 머지 후)
- feat/pr-g2-area (#12 머지 후)
- → GitHub repo 정리

---

## 14. 100점 시점 (헌법 §130)

> **PR-E ~ PR-O 머지** (Phase 1 완료) + **Track B 외부 검증** + **사용자 NPS 측정** = **2026 Q4 ~ 2027 Q1**

현재 점수: **93~95** (PR-E 머지로 진입). 100점까지 ~ 6~8개월 로드맵.

오늘 진행:
- ✅ 회귀 안전망 (헌법 §125 단계 1-8)
- ✅ 라이브 main 안정화
- ✅ 면적 거버넌스 (헌법 큐 외 — 사장님 도메인 통찰 보호)

남은 큰 작업:
- PR-A (type 정규화) → KPI #2 직접 달성
- PR-C (17 enrichment) → KPI #3-5 직접 달성
- PR-M / PR-N (접근성 + 모바일) → KPI #7 직접 달성
- PR-D (SEO) → KPI #8 직접 달성
- 4주 0 회귀 → KPI #9 직접 달성

---

## 15. v16 단 한 줄 비전

> *"이미 있는 것을 한계까지, UI 는 단 한 픽셀도 안 바꾸고 (사장님 명시 승인 X 한),  
> 회귀 0 의 자동 게이트 위에서, 사회초년생·신혼부부·사업자가  
> 100% 실매물을 직방보다 정확하게 만나는 — 그게 WISHES."*

(PR-G2-AREA 추가: 면적 정보 부족해도 광고 노출 / 사용자 부정적 표시 X / 마케팅 효과 보호)

---

작성: 2026-04-30 07:00 KST  
작성자: Claude (Cowork mode 세션)  
이전 인계: `_HANDOFF_2026-04-29_v15.md`  
다음 인계 (예정): `_HANDOFF_2026-XX-XX_v17.md` (PR-A 또는 PR-G2 머지 후)
