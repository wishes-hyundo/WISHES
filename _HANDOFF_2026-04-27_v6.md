# wishes.co.kr 인계 문서 v6 — 2026-04-27 (BoB Phase 1)

> **이전 인계**: `_HANDOFF_2026-04-27_v5.md`
> **이번 v6 시점**: BoB 새 가게 (/search/v2) 시작 + 빌드 hang 우회

---

## 0. TL;DR

| 작업 | 결과 |
|---|---|
| ✅ admin redirect 풀기 (옛날 기능 복원) | commit `e739667` (이전 라이브) |
| ✅ 사이드바 정리 | commit `260e34a` |
| ✅ 기능 카탈로그 | `outputs/admin_vs_search_feature_catalog.md` |
| ✅ BoB Phase 1 (shadcn + TanStack) push | commit `96ceaf9` (빌드 실패) |
| ✅ query-core type 충돌 fix | commit `3098be5` (빌드 19분 hang) |
| ⚠️ ListingTable 임시 stub | commit `d47fe1f` (빌드 우선) |

---

## 1. 사용자 핵심 명령 (이번 세션)

1. **"단 하나도 놓치지 마"** — admin/* 모든 기능 보존
2. **"2026 BoB 기술"** — content.js 13K줄 옛날 X
3. **"옛날 구방식 X"** — 새로 만들기
4. **"항시 SOTA 검증"**

→ **Strangler Fig 패턴** (글로벌 SaaS 표준 마이그레이션) + shadcn + TanStack Table.

---

## 2. BoB Phase 1 — /search/v2 신규

### 2-1. 의존성 25개 추가
- **@tanstack/react-table 8.21**
- **@radix-ui/* primitives** (12개)
- **class-variance-authority + tailwind-merge + tailwindcss-animate**
- **react-hook-form 7.55 + @hookform/resolvers 3.10**
- **sonner, cmdk, react-dropzone, vaul, xlsx, papaparse**

### 2-2. shadcn 셋업
- `src/lib/utils.ts` — twMerge 추가
- `tailwind.config.ts` — shadcn HSL theme + tailwindcss-animate
- `src/app/globals.css` — shadcn HSL 변수 (wishes 다크그린 매핑)
- `src/components/ui/`: button, input, badge, label, separator, table

### 2-3. /search/v2 라우트
- `layout.tsx` (125줄): 인증 + Sidebar
- `page.tsx` (83줄): 대시보드
- `listings/page.tsx` (79줄): RSC + Suspense
- `components/search-v2/ListingTable.tsx` (84줄, 임시 stub)

### 2-4. Strangler Fig 병행 운영
- 옛날 /search content.js + 옛날 /admin/* 그대로 유지
- 새 /search/v2 별도 라우트 (기능 추가 시 점진)

---

## 3. ⚠️ 빌드 이슈 + 우회

### 3-1. 첫 실패 — TanStack query-core nested type 충돌
```
Type error: Property '#private' in type 'QueryClient' refers to a different member
./src/components/providers/QueryProvider.tsx:91:7
```

**Fix**: `package.json overrides` `@tanstack/query-core: 5.99.2` + `as unknown as` 캐스트 → push `3098be5`.

### 3-2. 두 번째 — TanStack Table type 추론 OOM (빌드 19분 hang)
**원인**: TanStack Table v8 의 `ColumnDef<Listing>[]` 제네릭 추론 + 큰 codebase 동시 컴파일 → OOM/timeout.

**Fix**: ListingTable 을 임시 단순 `<div>` stub 으로 교체 (84줄) → push `d47fe1f`. **다음 단계**에서 점진 복원 (column 별 type assert 추가).

---

## 4. 라이브 commit 흐름 (이번 세션)

```
f5eff30 (인계 v5)
  ↓
0883f58 — v2 통합 + admin redirect (단순)
  ↓
260e34a — 사이드바 정리
  ↓
e739667 — admin redirect 풀기 (옛날 기능 복원)  ← 안정 라이브
  ↓
96ceaf9 — BoB Phase 1 [빌드 실패]
  ↓
3098be5 — query-core fix [빌드 19분 hang]
  ↓
d47fe1f — ListingTable 임시 stub [빌드 진행 중]
```

---

## 5. 사용자 검증 가능 (현재 라이브)

### ✅ 안정 동작
- `/search` 옛날 가게 + content.js + v300 v2 AI 재생성
- `/admin/listings`, `/admin/search`, `/admin/listings/new` (모든 기능 복원)
- 사이드바 (매물 메뉴 숨김, 중개사 포털 prominent)

### ⏳ d47fe1f 빌드 통과 시
- `/search/v2` 대시보드 + sidebar
- `/search/v2/listings` 매물 목록 (단순 stub, 12,000+ 매물)

---

## 6. 다음 세션 우선순위

### 🔴 즉시
1. d47fe1f 빌드 결과 확인 (Vercel 대시보드)
2. 빌드 통과 시 → /search/v2 라이브 검증
3. 빌드 또 실패 시 → 추가 단순화 (page 자체 stub)

### 🟡 Phase 1.5 (TanStack Table 정식 복원)
- ListingTable 의 column 별 type 명시적 assert
- TanStack Virtual 복원 (가상 스크롤 12K 매물)
- 검색/정렬/필터

### 🟢 Phase 2 (검수 + 매물 등록)
- shadcn Sheet 패널 (매물 검수, v2 AI 재생성 통합)
- shadcn Stepper (매물 등록 6 step)
- React Hook Form + Zod
- 사진 업로드 (react-dropzone)
- 주소 검색 (다음)
- 건축물대장 자동 조회

### 🟢 Phase 3 (일괄 작업)
- TanStack Table row selection
- 일괄 상태/삭제/검증
- CSV 내보내기 (xlsx)
- 일괄 업로드 (papaparse)

### 🟢 Phase 4 (상세보기 강화)
- 교통정보 (카카오 directions)
- 실거래가 트렌드
- 매물 검수 모달 (sheet)

### 🟢 Phase 5 (옛날 가게 안전 제거)
- 100% 검증 후 content.js 13K + admin/* 5.5K 줄 완전 삭제
- 사이드바에서 admin 링크 제거
- /search/v2 → /search 로 이전

---

## 7. 솔직한 한계

1. **TanStack Table v8 + 큰 codebase = type checker 부담** — Phase 1.5 에서 명시적 type assert 로 해결
2. **사용자 명령 "단 하나도 놓치지" 100% 보존**: 4~7일 작업 (5 Phase)
3. **이번 세션 = Phase 1 + 빌드 이슈 우회 (1/5)**
4. **content.js 13K 완전 대체** = 점진 (Strangler Fig)

---

## 8. 알려진 워크플로우 이슈

- TanStack Table v8 + Next.js 15 + 큰 codebase = 빌드 type checker 19분 hang
- 우회: column 별 type 명시적 assert 또는 stub 으로 시작
- Edit 도구 truncate (이번 세션 5번 발생) — heredoc 우회

---

## 9. 한 줄 요약

**BoB Phase 1 (shadcn + TanStack) 인프라 push 완료. 빌드 이슈 2건 우회. 라이브는 안정 (옛날 가게 모두 동작).**
**다음: d47fe1f 빌드 검증 → /search/v2 라이브 → Phase 1.5~5 점진.**

작성: 2026-04-27 v6 (라이브 e739667 안정, d47fe1f 빌드 진행)
