# wishes.co.kr 인계 v15 — 2026-04-29 (4가지 영구 요구사항 라이브 반영)

> 이전: v14. 이번: 사장님 4가지 영구 명령 + 매물카드 통합 + 폴리곤 성능 + 비로그인 위치 보호.

---

## 0. 한 줄 요약

**사장님 4가지 영구 요구사항 라이브 반영. /map?listing=ID → /map/53190 path. /listings 영구 폐기. 매물카드 본문 + 5-2-10-1 + footer 통합. 폴리곤 성능 220→140ms. 비로그인 위치 동그라미.**

---

## 1. 라이브 (commit `f55d3ae` — 사장님 작업 + 본 세션 commit 포함)

| 항목 | URL | 동작 |
|---|---|---|
| 매물카드 path | `/map/53190` | ✅ middleware rewrite + URL 동기화 + flyTo |
| /listings redirect | `/listings/53190` | ✅ 308 → `/map/53190` |
| AI 검색 (매물번호) | 검색창 `53190` | ✅ openListingDetail |
| AI 검색 (주소) | 검색창 `서울 관악구 신림동` | ✅ Kakao Geocoder flyTo |
| AI 검색 (자연어) | 검색창 `신림 투룸 3억 이하` | ✅ /api/map/search-nl 규칙 파서 |
| 매물카드 본문 | `/map/53190` 진입 | ✅ 본문 + 5-2-10-1 + footer 모두 스크롤 영역 안 |
| 비로그인 위치 | 매물카드 미니맵 | ✅ Circle 100m + 주소 X |
| 추천 매물 | 매물카드 5번 | ✅ 썸네일 + 정보 2개 compact |
| 공유 버튼 | 매물카드 footer 우측 | ✅ 아이콘만 작게 |
| 폴리곤/매물 성능 | viewport API | ✅ 220→140ms warm + Edge cache HIT |

---

## 2. 본 세션 핵심 commit (시간순)

| commit | 내용 |
|---|---|
| f9bf3c1 | feat(map): /map?listing=ID URL 라우팅 — useListingUrlSync.ts 신규 |
| 4f38543 | fix(map): /map/<ID> 직접 진입 시 매물 카드 표시 — fallback fetch |
| 6fb8f83 | fix(map): /map/<ID> 진입 시 매물 위치로 지도 자동 flyTo |
| ff7fa44 | feat(map): URL path 형식 — /map/53190 (middleware rewrite) |
| cfc2b17 | fix(map): /map/<숫자> 도 isMapPage 인식 — 레이아웃 깨짐 fix |
| 053f7c0 | feat(search): 검색창 1개 트라이어드 — 매물번호+주소+자연어 |
| 58bac9f | fix(map): /listings redirect → /map/:id (next.config.js) |
| ab682e7 | feat(map): Step 2 — 본문 끝 1-10-2-5 (위→아래) |
| 6fffd0f | fix(map): Step 2 본문 사라짐 fix — 4개 섹션 스크롤 영역 안으로 |
| bf91cad | feat(map): Step 4 — 공유 버튼 아이콘만 |
| 470ff40 | feat(map): Step 5 — 매물 위치 비로그인 동그라미 + 주소 숨김 |
| 4ee9bc0 | feat(map): Step 6 — 추천 매물 썸네일 + 정보 추가 |
| 522746a | perf(viewport): P1 — JWT auth + count timeout 단축 |
| 3def5d5 | perf(viewport): P2 — Edge cache 강화 |

---

## 3. CLAUDE.md 영구 정책 갱신 (이번 세션 추가)

§ `/map` 검색·라우팅·성능 (사장님 명령 2026-04-29):

1. **AI 자연어 검색 활성화** — 검색창 1개로 매물번호 / 주소 / 자연어 모두
2. **/listings 영구 폐기** — `/listings/:id` → `/map/:id` 301 redirect
3. **매물카드 URL 라우팅** — `/map/53190` path 형식 (middleware rewrite)
4. **폴리곤/매물 렌더링 딜레이 0** — viewport API < 150ms warm + Edge cache HIT

부트스트랩 §4 "AI 자연어 검색 X" 무효화 (Gemini Flash 무료 한도).
부트스트랩 §11 "/listings 유지" 영구 폐기.

---

## 4. 매물카드 (ListingDetailModal) 최종 구조

```
┌─────────────────────────────┐
│ HERO (220px)                │ ← 갤러리 사진만 (영상 통합 보류)
│ - 사진 fetch                 │
│ - lightbox 풀스크린          │
│ - 영상 배지 (listing.has_video)│
├─────────────────────────────┤
│ ↓ 스크롤 영역 (flex-1) ↓    │
│ 헤더 (가격/주소/ai_title)    │ ← 본문 절대 유지
│ 기본정보 (전용/층/방수)      │
│ 매물정보 테이블             │
│ 내부 시설 옵션              │
│ 보증 검증 4단              │
│ ───────────────            │
│ 1) 실거래가 차트            │ ← 5-2-10-1 위→아래
│ 10) 주변 교통 (지하철 4개)  │
│ 2) 매물 위치 미니맵         │   비로그인=Circle 100m
│ 5) 추천 매물 (썸네일+정보 2개) │
│ ↑ 스크롤 영역 끝 ↑          │
├─────────────────────────────┤
│ [담당자 연결] [🔗]          │ ← 공유 우측 작게
└─────────────────────────────┘
```

---

## 5. 미완 / 보류

### Step 3 — 갤러리 영상+사진 통합 (빌드 실패 → revert)
- 사장님 명령: 영상 → 대표사진 → 사진 순서, 첫 슬라이드 = 대표사진
- 빌드 실패 원인 미상 (Vercel 빌드 로그 접근 불가)
- 변경량 큰 commit (galleryImages: string[] → galleryItems: object[])
- **재시도 plan**: 3a/3b/3c 분할 — 영상 fetch / hero 분기 / lightbox 분기

### 폴리곤 성능 추가 개선 (UI 명령 한계 도달)
- 220ms → 140ms (-36%) 달성
- 추가 개선은 사장님 "기능 X UI X" 명령 위반 가능
  - ❌ 이미지 복원 비동기화 (UI 영향 가능)
  - ❌ 클라이언트 debounce 250→60ms (기능 변경 ground)
  - 가능: 마커 전용 endpoint 분리 (마커/카드 별개 fetch) — 큰 변경

---

## 6. 다음 세션 액션

### 우선순위 1: Step 3 갤러리 분할 재시도
1. **3a** — 영상 fetch state 추가 (UI 변경 0, 데이터 준비만)
2. **3b** — hero 영상 분기 렌더 (작은 변경, video tag)
3. **3c** — lightbox 영상 분기 (작은 변경)

각 단계 단독 commit + Vercel 빌드 검증.

### 우선순위 2: 사장님 라이브 체감 검증
- 폴리곤 클릭 → 매물 표시 딜레이 (이전 답답 → 지금 어떻게)
- 매물카드 본문 + 5-2-10-1 모두 정상
- 비로그인 위치 동그라미 정상

### 우선순위 3: 데이터 자동화 (이전 인계 v14 참조)
- env 등록 (RTMS, V-WORLD, NTS, POLICE, R2)
- cron 23개 가동 (현재 상태 점검 필요)
- 한국 17 데이터 enrich (이전 v14에서 시작, 일부만 백필)

---

## 7. 빌드 검증 패턴

1. `/sessions/.../tmp/wishes-build` 새 clone (origin/main 최신)
2. 변경 적용 — **mount cp 절대 X (사장님 최신 덮어쓸 위험)**
3. 단독 commit (한꺼번에 X)
4. push 후 60-90초 대기 → curl /api/version 확인
5. 빌드 실패 시 **즉시** git revert

### 위험 패턴 (이번 세션 발견)
- mount의 outdated 파일을 새 clone에 cp → 사장님 최신 변경 덮어쓰기 (530b57a 빌드 실패 원인)
- 큰 변경 단독 commit → ESLint/TS 충돌 추적 어려움 (Step 3 빌드 실패)

---

## 8. 사장님 명령 정리 (이번 세션)

### 절대 X (영구)
- /search 손대지 마라 (vanilla content.js 영구 보존)
- 매물카드 본문 절대 유지 (기능/UI X)
- 사장님 직접 검토 페이지 X
- 큰 비용 X
- 거래 기록 삭제 X (PIPA)
- 카톡 알림톡 (Solapi) X — Resend 이메일만

### 명령 (이번 세션)
- 매물카드 본문 안 끼워넣기 (X — 본문 끝에 별도 섹션)
- 5-2-10-1 위→아래 (1번이 위, 5번이 아래)
- 공유 버튼 우측 작게 아이콘만
- 비로그인 위치 동그라미 + 주소 X
- 추천 매물 썸네일 + 정보
- 영상 → 대표사진 → 사진 (썸네일은 대표사진)
- 폴리곤/매물 딜레이 0 (UI/기능 절대 X)

---

## 9. 시작 명령

### 이어서 진행
- "이어서" → Step 3 갤러리 분할 (3a/3b/3c 순차)

### 라이브 점검
- "라이브 점검" → /api/version + viewport 측정 + 4가지 동작 확인

### 사장님 사용 후 피드백
- "사장님 사용해보고 피드백" → 라이브 체감 후 우선순위 재조정

---

작성: 2026-04-29  
이전 인계: _HANDOFF_2026-04-28_v14.md  
주요 commit: f9bf3c1, ff7fa44, ab682e7, 470ff40, 4ee9bc0, 522746a, 3def5d5
