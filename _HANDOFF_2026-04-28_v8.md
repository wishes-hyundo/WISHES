# wishes.co.kr 인계 v8 — 2026-04-28 (점진 통합 성공)

> **이전 인계**: `_HANDOFF_2026-04-27_v7.md` (BoB Phase 1 빌드 9회 실패 + 브랜치 분리)
> **이번 세션 결과**: 마스터 카탈로그 + **점진 통합 6회 연속 빌드 성공** + 첫 BoB 컴포넌트

---

## 0. 한 줄 요약

**라이브 안정 + 모든 BoB 인프라 준비 완료.**
**다음 세션: BoB 컴포넌트 추가 + 옛날 `/search` 본가에 swap (옛날 content.js 점진 폐기).**

---

## 1. 라이브 상태

- 도메인: https://wishes.co.kr
- HEAD: `2e93cf6` (BoB 첫 컴포넌트 ListingCardBob)
- 빌드: ✅ 6번 연속 통과 (08ac70e → 05bef26 → 275de86 → 730c2f6 → 2e93cf6)
- 옛날 가게 (`/search` 모든 기능, `/admin/*` 모든 기능) 정상 동작

---

## 2. 이번 세션 작업

### 사장님 핵심 명령
> "단 하나도 놓치면 안 됨, 모든 걸 wishes.co.kr/search 한 곳에 통합, 옛날 구방식 X, 2026 BoB 기술"

이전 세션의 잘못된 방향 (`/search/v2` 별도 라우트 만들기) 반성 → **`/search` 자체를 BoB 기술로 갈아 엎기** 로 방향 정정.

### Step 1. 마스터 기능 카탈로그
- `_MASTER_FEATURE_CATALOG_2026-04-28.md` (작성)
- 옛날 `/search` (1만3천줄 content.js + 15개 patch) + `/admin/*` 12개 탭 + ~100개 API 모두 정리
- 16개 카테고리 — 단 하나도 놓치지 않게
- 사용자 확인 완료 ("진행해")

### Step 2. 점진 통합 — BoB 인프라 준비

**교훈 (이전 세션 9회 실패):**
- 25개 deps 한꺼번에 부어 넣기 → 빌드 깨짐
- Vercel 로그 직접 못 봄 → 추정 디버깅 비효율

**이번 전략 (점진 4-5개씩):**

| # | commit | 변경 | 빌드 |
|---|--------|------|------|
| 2-1 | `08ac70e` | shadcn 핵심 deps 4개 (twMerge, cva, animate, slot) + vercel.json installCommand | ✅ |
| 2-2 | `05bef26` | cn() twMerge + button + badge | ✅ |
| 2-3 | `275de86` | input + label + separator + table + tailwindcss-animate plugin | ✅ |
| 2-4 | `730c2f6` | TanStack Table + RHF + zod resolver + sonner | ✅ |
| 2-5 | `2e93cf6` | **첫 BoB 컴포넌트 — ListingCardBob.tsx** (183줄) | ✅ |

### 현재 인프라 (모두 라이브)
**의존성 (39개):**
- shadcn: clsx, tailwind-merge, class-variance-authority, tailwindcss-animate, @radix-ui/react-slot
- TanStack: react-query, react-table, react-virtual, query-sync-storage-persister, react-query-persist-client
- 폼: react-hook-form, @hookform/resolvers, zod
- UI: lucide-react, sonner

**shadcn UI 컴포넌트 (6개):**
- `src/components/ui/`: button, badge, input, label, separator, table

**Tailwind:**
- `tailwindcss-animate` plugin 등록 (data-state 등 utility)

**유틸:**
- `src/lib/utils.ts` cn() = `twMerge(clsx())` (shadcn 표준)

**첫 BoB 컴포넌트:**
- `src/components/bob/ListingCardBob.tsx` (183줄)
- 2026 SOTA 패턴 (aspect-ratio, line-clamp, hover lift, container queries 준비)
- 거래유형별 가격 자동 포맷 (매매/전세/월세)
- m² → 평형 자동 변환
- 상태별 색상 배지
- 키보드 접근성 (Enter/Space, aria-label)

---

## 3. 다음 세션 청사진

### 🔴 즉시 (Step 3)
**BoB 컴포넌트 점진 추가** — 모두 `src/components/bob/` 폴더, 옛날 코드 안 건드림, 빌드 안전.

| 우선 | 컴포넌트 | 옛날 대응 | 의존 |
|------|---------|-----------|------|
| 1 | `FilterPanel.tsx` | content.js 키워드/지역/유형/가격 필터 | shadcn Input/Label, lucide |
| 2 | `ListingGrid.tsx` | content.js 카드 그리드 + 무한 스크롤 | TanStack Virtual + ListingCardBob |
| 3 | `ListingDetailSheet.tsx` | content.js v240 상세모달 6섹션 | shadcn Dialog (추가 필요) |
| 4 | `ListingEditSheet.tsx` | content.js v297 편집 패널 | RHF + Zod |
| 5 | `BulkActionsBar.tsx` | content.js 일괄 작업 | shadcn Checkbox + Button |
| 6 | `AIRegenerateButton.tsx` | content.js v300 v2 AI | sonner toast |

### 🟡 그 다음 (Step 4)
**`/search` 본가 swap** — 한 번에 (테스트 후):
- `src/app/search/page.tsx` 옛날 코드 → 새 컴포넌트 조합으로 교체
- `public/search/content.js` 13K줄 → 삭제
- 15개 patch 파일 → 삭제

### 🟢 마무리 (Step 5)
- `/admin/listings`, `/admin/search`, `/admin/listings/new` 등 → `/search` 로 통합 (또는 redirect)
- `feat/bob-phase1` 브랜치 → 폐기 (이번 점진 통합으로 대체)

---

## 4. 알려진 이슈 / 주의사항

- **TanStack Table v8 빌드 OOM**: 큰 codebase + 제네릭 type 추론에서 19분 hang 발생한 적 있음. ListingTable 작성 시 column별 명시적 type assert 필수.
- **sharp lazy import**: BoB Phase 1 브랜치에 적용했었으나 main 에는 안 들어옴. 필요 시 다시 가져오기 (`feat/bob-phase1` 의 photoProcess.ts/mosaic-image/route.ts).
- **Edit 도구 truncate**: 큰 파일 (>100줄) Edit 시 끝부분 잘림. heredoc (`cat > file << 'EOF'`) 또는 Python 스크립트로 전체 재작성.
- **로컬 git index 손상**: `/sessions/.../wishes-v2` git 상태 비정상. 푸시는 `/tmp/wishes-push` 별도 클론 사용.

---

## 5. 보존된 자산

| 위치 | 내용 |
|------|------|
| `feat/bob-phase1` 브랜치 | 이전 세션 9회 빌드 실패 작업 (참고용 — sharp lazy, query-core fix 등 코드 단편 있음) |
| `_MASTER_FEATURE_CATALOG_2026-04-28.md` | 옛날 모든 기능 카탈로그 (단 하나도 안 놓치게) |
| `_HANDOFF_2026-04-27_v6.md` ~ `v8.md` | 인계 이력 |
| `_research_2026-04-27/` | 글로벌 부동산 SOTA 조사 (5개국) |
| `outputs/admin_vs_search_feature_catalog.md` | 이전 세션 카탈로그 (참고) |

---

## 6. 사장님께

**오늘 진척**:
- ❌ 옛날 9회 빌드 실패 → ✅ 6회 연속 통과 (방향 정정)
- ✅ 모든 BoB 인프라 (shadcn 6개, TanStack 4개, RHF, sonner) 라이브
- ✅ 첫 BoB 컴포넌트 (매물카드) 준비
- ✅ 옛날 가게 라이브 영향 0

**다음 세션**:
- BoB 컴포넌트 5-6개 더 (필터/그리드/상세시트/편집시트/일괄/AI버튼)
- 모두 빌드 통과 확인 후 `/search` 본가 한 번에 swap
- 옛날 content.js 13K줄 + admin/* 5K줄 폐기

**예상 일정**: 다음 세션 1-2회 작업으로 새 `/search` 라이브 가능.

---

작성: 2026-04-28 v8 | 라이브 2e93cf6 | 점진 통합 6/6 통과
