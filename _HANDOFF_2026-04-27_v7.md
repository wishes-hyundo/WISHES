# wishes.co.kr 인계 문서 v7 — 2026-04-27 (BoB Phase 1 → 브랜치 분리)

> **이전 인계**: `_HANDOFF_2026-04-27_v6.md` (BoB Phase 1 푸시 + 빌드 실패)
> **이번 v7 시점**: 9회 빌드 실패 → main 안정 복원 (e739667), BoB Phase 1 → `feat/bob-phase1` 브랜치 보존

---

## 0. 한 줄 요약

**main 라이브 = `e739667` (안정, 옛날 가게 모두 동작).**
**BoB Phase 1 작업 100% 보존 = `feat/bob-phase1` 브랜치 (HEAD `7656bd8`).**
**다음 세션: 로컬 `npm run build` 로 진짜 빌드 실패 원인 파악 후 점진 통합.**

---

## 1. 라이브 상태 (사용자 영향 없음)

- 도메인: https://wishes.co.kr
- 현재 commit: **`e739667`** (revert(integration): admin redirect 제거)
- 빌드: ✅ 성공 (마지막으로 통과한 커밋)

### 동작 확인된 라우트
- `/` 홈
- `/search` 옛날 가게 (content.js + v300 v2 AI 재생성 통합)
- `/admin/listings` (모든 기능 보존)
- `/admin/search` (옛날 admin 검색)
- `/admin/listings/new` (매물 등록)
- 사이드바: 매물 메뉴 숨김, 중개사 포털 위치 이동 완료

---

## 2. 빌드 실패 패턴 (9회)

| commit  | 변경 내용 | Vercel 결과 |
|---------|---------|-----------|
| `96ceaf9` | feat(BoB-Phase1) /search/v2 신규 (shadcn + TanStack 25 deps) | ❌ failure |
| `3098be5` | fix query-core type 충돌 | ❌ failure |
| `d47fe1f` | ListingTable stub (TanStack Table type OOM 우회) | ❌ failure |
| `eea8232` | docs handoff v6 | ❌ failure |
| `9a49b78` | sharp linux-x64 binary optionalDependencies 추가 | ❌ failure |
| `a9289ff` | build script 원복 | ❌ failure |
| `f56b620` | sharp 강제 install (vercel.json + .npmrc + dependencies 이동) | ❌ failure |
| `7656bd8` | sharp lazy import (dynamic import 패턴) | ❌ failure |
| `dd3f1ef` | photoProcess + mosaic-image truncation 복원 | ❌ failure |

**진짜 원인**: 미확인. Vercel 로그 직접 접근 불가 (CLI 인증 없음, 웹 페이지는 React-rendered).
사용자가 이전 세션에서 캡처한 로그 = sharp module 에러였음. 현재 로그는 다를 수 있음.

---

## 3. 보존된 작업 (`feat/bob-phase1` 브랜치)

```bash
git checkout feat/bob-phase1
# HEAD = 7656bd8
```

### 포함 내용 (commit 96ceaf9 ~ 7656bd8)
- ✅ `package.json` — 25 새 deps (shadcn + TanStack 8.21 + radix-ui 12개 + RHF + Zod + xlsx + papaparse + sonner + cmdk + react-dropzone + vaul)
- ✅ `tailwind.config.ts` — shadcn HSL CSS variables + tailwindcss-animate
- ✅ `src/app/globals.css` — shadcn HSL 변수 (wishes 다크그린 매핑)
- ✅ `src/lib/utils.ts` — `twMerge` 추가
- ✅ `src/components/ui/` — 6개 (button, input, badge, label, separator, table)
- ✅ `src/app/search/v2/layout.tsx` — 인증 + Sidebar
- ✅ `src/app/search/v2/page.tsx` — 대시보드
- ✅ `src/app/search/v2/listings/page.tsx` — RSC + Suspense
- ✅ `src/components/search-v2/ListingTable.tsx` — 84줄 stub (TanStack Table OOM 우회)
- ✅ `src/components/providers/QueryProvider.tsx` — query-core type 캐스트
- ✅ `src/lib/photoProcess.ts` — sharp lazy import (dynamic import 패턴)
- ✅ `src/app/api/mosaic-image/route.ts` — sharp lazy import
- ✅ `vercel.json` — installCommand `npm install --include=optional --no-audit --no-fund`
- ✅ `.npmrc` — `include=optional`

### 디버그 시 확인할 점
1. **로컬 `npm install && npm run build` 실행** — Vercel 로그 대신 로컬에서 정확한 에러 메시지 캡처
2. 25개 deps 중 어느 것이 빌드를 깨는지 — bisect (5개씩 분할 commit)
3. shadcn UI 단독 — TanStack 빼고 시도
4. 새 라우트 `/search/v2` — 단순 placeholder 로 시작

---

## 4. 다음 세션 제안 진행

### 🔴 0순위 — 빌드 실패 원인 파악
1. `git checkout feat/bob-phase1`
2. 로컬에서 `npm install` (15분 소요 예상)
3. `npm run build` (Vercel 로그 대신 정확한 에러 메시지 확보)
4. 에러 픽스 → main merge

### 🟡 대안 — 점진 통합
1. main 에서 시작 (e739667 안정)
2. shadcn UI 컴포넌트만 (deps: clsx, tailwind-merge, class-variance-authority, tailwindcss-animate, radix-ui-slot) → push → 빌드 확인
3. shadcn 통과 시 → /search/v2 placeholder 라우트 → push → 빌드 확인
4. 그 후 → TanStack Query → push → 빌드 확인
5. 그 후 → TanStack Table 점진 (column 별 type assert) → push → 빌드 확인

각 단계마다 빌드 통과 보장.

### 🟢 우선순위 변경 가능
사용자 우선순위 따라 BoB Phase 1 미루고 다른 일 (Phase 2 매물 등록, 검수, 일괄 작업) 먼저 진행해도 됨. 옛날 가게는 모든 기능 동작 중.

---

## 5. 알려진 이슈

- **Edit 도구 truncate**: 큰 파일 (100+ 줄) Edit 시 끝부분이 잘림. 8회 발생 누적.
  - 우회: `cat > file << 'EOF'` 또는 Python `open().write()` 로 전체 재작성.
- **Vercel CLI 인증 없음**: 빌드 로그 직접 접근 불가. GitHub commit status 만 보임.
- **로컬 git index 손상**: `/sessions/.../wishes-v2` 의 index 가 비어 있음 (모든 파일 untracked 표시).
  파일 자체는 정상. 푸시는 `/tmp/wishes-push` 별도 클론 사용.

---

## 6. 솔직한 한계 + 사용자께

이번 세션 결과:
- ✅ 옛날 가게 라이브 안정 유지
- ✅ BoB Phase 1 코드 100% 보존 (브랜치)
- ✅ 인프라 분석 + 다음 단계 명확화
- ❌ /search/v2 라이브 미배포 (빌드 실패 9회)

**추천**: 다음 세션에서 로컬 `npm run build` 한 번이면 정확한 원인 파악 가능.
지금처럼 push → Vercel 로그 못 봄 → 추정 → push 반복은 비효율.

---

작성: 2026-04-27 v7 (라이브 e739667 안정, BoB Phase 1 → feat/bob-phase1)
