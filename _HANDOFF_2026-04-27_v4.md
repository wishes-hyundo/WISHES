# wishes.co.kr 인계 문서 v4 — 2026-04-27 (마라톤 세션)

> **이전 인계**: `_HANDOFF_2026-04-27_v3.md` (Phase 0-D cascade 까지)
> **이번 v4 시점**: SEO 회복 + ai_description v2 시스템 (글로벌 SOTA) + Gemini API 키 셋업 완료

---

## 0. TL;DR

| 영역 | 결과 |
|---|---|
| 🚨 보안 자물쇠 | ✅ 4개 (DB 직접) |
| 🔥 파싱 버그 | ✅ 12,122건 (DB 직접) |
| 🥈 건축물대장 cron | ✅ commit `8bd5170`, 매 30분 25건 자동 (5건 적용 검증 완료) |
| 🎯 type 정정 | ✅ 564건 (사용자 발견 46339 fix) |
| 🔗 cascade DB | ✅ field_sources jsonb (Phase 0-D) |
| 🔐 Vercel CRON_SECRET | ✅ Chrome 자동 추가 + Redeploy |
| 📈 SEO 회복 | ✅ commit `1fc6684` + `29b6a72` (noindex 조건 변경 + JSON-LD + sitemap 재제출) |
| 🤖 ai_description v2 시스템 | ✅ commit `4378b6b` (RAG + 7스타일 + 환각 검출) |
| 🔑 Gemini API 키 | ✅ Chrome 자동 발급 + Vercel GEMINI_API_KEY 추가 + Redeploy |

라이브 commit: **`4378b6b`**

---

## 1. 새로 만든 시스템 — ai_description v2

### 1-1. 배경
사용자 발견: 매물 46340 의 기존 ai_description 에 "서울대입구역" (실제 신림역), "세브란스병원" (실제 신촌) 등 환각.
원인: 기존 generate-description prompt 가 "지역 환경 **추정**해서 작성" → LLM 환각 유발.

### 1-2. 글로벌 조사
`outputs/real_estate_listing_report_2026.md` (2,200단어) — Zillow/Redfin/Rightmove/SUUMO/Beike/PropertyGuru 7개 SOTA 패턴 + 카피 학술 데이터 + 2026 LLM 비교 + RAG + 다양성 기법 종합.

### 1-3. v2 시스템 (commit `4378b6b`)

**파일 4개 신규**:
- `src/lib/listing-rag.ts` (218줄): RAG context 빌더
  - 검증된 사실만 추출 (gu/dong/building_name/station)
  - 매물별 차별점 hook 자동 추출 (수리완료/코너세대/남향 등 25+ 키워드)
  - target segment 자동 분류 (single/couple/family/business/investor)
  - forbidden_topics 리스트 (가격/면적/옵션 — 표 정보 중복 금지)
  - hash 기반 style_index (golden ratio)
- `src/lib/listing-styles.ts` (137줄): 7개 페르소나 풀
  - 동네 베테랑 / 직설 정보 / 발견 1인칭 / 비교 분석 / 라이프 감성 / 컨설턴트 / Q&A
  - 12개 헤드라인 시작 패턴 풀
  - hash 기반 결정적 선택 (같은 매물 = 같은 스타일)
- `src/lib/listing-verify.ts` (189줄): 4단 검증
  - AI 정형구 검출 (88종)
  - 표 정보 중복 검출 (가격/면적/방수/주차대수)
  - 환각 단어 검출 (KNOWN_STATIONS 60+ + KNOWN_LANDMARKS)
  - 길이 검증
- `src/app/api/admin/generate-description-v2/route.ts` (325줄): 메인 라우트
  - Gemini 2.5 Flash 우선 (무료 일 100K)
  - Anthropic Haiku fallback
  - Temperature 0.9, Top_P 0.9
  - JSON 응답 + verify 결과 동봉

### 1-4. 환경변수 셋업 완료 (Vercel)
- `GEMINI_API_KEY` = `AIzaSyCHibAK5iW9q-j0qpehDYE06e33a5X5v8E` ✅
  - Google AI Studio 자동 발급 (이번 세션)
  - 무료 등급 (일 100K 토큰)
  - 프로젝트: gen-lang-client-0078359666
- `ANTHROPIC_API_KEY` ✅ (이미 있음, fallback 용)

### 1-5. 호출 방법
```bash
POST https://wishes.co.kr/api/admin/generate-description-v2
Headers: Authorization (admin)
Body: { "listingId": 46340 }

Response:
{
  "success": true,
  "title": "...",
  "description": "...",
  "keywords": [...],
  "tags": [...],
  "meta_description": "...",
  "style": { "id": "veteran", "name": "동네 베테랑" },
  "headline_pattern": "역명/노선 선두",
  "used_llm": "gemini-2.5-flash",
  "verify": {
    "title": { "ok": true, "ai_banned": [], ... },
    "description": { "ok": true, ... },
    "passed": true
  }
}
```

### 1-6. 다음 세션 — 검수 UI 추가 필요
v2 는 매물 ID 기반 (기존 매물 검수용). `/admin/listings/[id]` 또는 `/admin/search` 에 "v2 AI 재생성" 버튼 추가하면 사용자 클릭으로 동작 검증 가능.

`/admin/listings/new` 페이지 (새 매물 등록) 의 generate-description 호출은 v1 그대로 유지 (form 기반).

---

## 2. SEO 회복 (commit `1fc6684`, `29b6a72`)

### 2-1. 원인
공실클럽 source_site 있는 매물 = 모두 noindex → 12,114/12,115 매물 색인 차단 → 구글 "NOINDEX 메일".

### 2-2. 수정
- `listings/[id]/page.tsx`: `isCrawled` → `hasOwnContent` (ai_description/description/seo_meta_description 30자+ 있으면 색인 허용)
- `sitemap.ts`: 같은 hasOwnContent 필터 → 색인 OK 매물만 sitemap 등록
- RealEstateListing JSON-LD 매물별 inject (구글 리치 결과)

### 2-3. 효과
- 즉시 색인 가능: **2,876건 (24%)** → 600건 자동생성 추가 → **3,476건**
- sitemap URL: 12,123 → **1,635** (실제 색인 가능 매물만, 노이즈 제거)
- 매물 46339 (사용자 발견) 검증: `noindex=0`, `index, follow`, JSON-LD 정상

### 2-4. 사용자 한가할 때 (5분, 클릭만)
- ✅ Google Search Console sitemap 재제출 — 이번 세션 자동 완료
- ⏳ **네이버 검색어드바이저** sitemap 제출 — `_NAVER_SEO_SETUP.md` 참조 (Chrome MCP 안전 제한으로 자동화 X, 사용자 직접 클릭)

---

## 3. ai_description 600건 처리 (사용자 결정 대기)

자동 cron 으로 의도치 않게 생성된 600건 (raw 템플릿, 환각 X) — 처리 옵션:

### 옵션 A — 모두 NULL reset (사용자 정책 100% 반영)
```sql
UPDATE listings 
SET ai_description = NULL, 
    seo_meta_description = NULL,
    field_sources = field_sources - 'ai_description' - 'seo_meta_description'
WHERE field_sources->>'ai_description' = 'auto';
```

### 옵션 B — 600건 그대로 + v2 시스템으로 점진 교체
- 중개사가 매물 검수 시 v2 호출로 더 자연스러운 카피로 교체
- 환각 위험 0 (raw 템플릿)

### 옵션 C — 모든 ai_description NULL reset (기존 2,322 Anthropic 환각 매물 포함)
- 가장 깨끗한 상태에서 v2 로 재구축

**추천**: 옵션 C. 기존 Anthropic 환각 매물이 라이브에 떠있는 게 더 큰 신뢰 손실. 사용자 시간될 때 결정.

---

## 4. 자동화 동작 중 (사용자 추가 액션 0)

| cron | 주기 | 동작 |
|---|---|---|
| `backfill-building-info` | 매 30분 25건 | 카카오+data.go.kr → listings.building_info, building_name, built_year, building_purpose 자동 |
| 일 1,200건 → 약 10일에 11,500건 backfill 완료 | | |

검증: 매물 58095/58096/58097/58098/58099 backfill 완료 (이번 세션 수동 호출 5건)

---

## 5. 사용자 한가할 때 처리 사항

### 5-1. ai_description 600건 처리 결정 (옵션 A/B/C)

### 5-2. 네이버 검색어드바이저 sitemap 제출 (`_NAVER_SEO_SETUP.md`)

### 5-3. (선택) Supabase 보안 leftover
- `Auth → Attack Protection` 의 leaked password 보호 (SMTP 설정 후)
- spatial_ref_sys RLS (Supabase support 요청)

---

## 6. 다음 세션 우선순위

### 6-1. 즉시 (1~3시간)
- v2 라우트 라이브 검증 — admin 로그인 후 매물 ID 한 건 호출, 결과 확인
- `/admin/listings/[id]` 또는 `/admin/search` 검수 화면에 "v2 AI 재생성" 버튼 추가
- 600건 처리 옵션 결정 + 적용

### 6-2. cascade 완성 (반나절)
- admin PATCH 라우트 (`/api/admin/listings/[id]`) → 변경된 칸 field_sources['X']='broker' 자동 표시
- 이게 있어야 cascade 가 진짜 동작 (broker 잠금 칸 보호)

### 6-3. 한국 정부 API 추가 (1~2주)
- RTMS 실거래가 → 매물 가격 검증 + 시세 그래프
- 국세청 사업자번호 진위확인 → 중개사 가입 검증
- HUG 안심전세 → 깡통전세 자동 경고

### 6-4. 보안 잔여
- appointments/contacts/saved_searches/short_urls anon INSERT 차단 (폼 코드 점검 후)
- function search_path 12개 (한꺼번에 ALTER FUNCTION)
- HaveIBeenPwned 활성화 (Supabase Auth)

### 6-5. v2 시스템 보강 (Phase 2)
- 카카오 directions API → 정확한 지하철역 거리
- OSM Overpass → 도보 5분 내 편의점/마트/카페 진짜 개수
- enrichment jsonb 컬럼 (사실 캐시)

---

## 7. 라이브 commit 흐름 (이번 세션)

```
69fb892 (시작)
  ↓
8bd5170 — building-info cron 라우트 + 보안 자물쇠
  ↓
89b88af — cascade DB 구조 + type 정정 + cron cascade
  ↓
5c7979b — generate-descriptions cron 자동 호출 비활성화
  ↓
1fc6684 — SEO L-seo1 noindex 조건 변경 + sitemap 필터
  ↓
29b6a72 — SEO L-seo2 RealEstateListing JSON-LD
  ↓
379acd7 — generate-descriptions cron 추가 (이후 비활성화됨)
  ↓
4378b6b — ai-desc v2 시스템 (RAG + 7스타일 + 환각 검출)  ← 라이브
```

---

## 8. DB 직접 적용된 마이그레이션 (이번 세션)

Supabase MCP `apply_migration` 으로 production 직접 적용 — supabase/migrations/ 폴더에는 SQL 파일 없음.

| 이름 | 내용 |
|---|---|
| `phase0b_anon_listings_lockdown_2026_04_27` | DROP 4 anon policies (보안) |
| `phase0a_parsing_bug_fix_2026_04_27` | 12,122건 raw_fields → listings 재매핑 |
| `phase0c_type_correction_2026_04_27` | 본문 헤더 → type 정정 564건 |
| `phase0d_field_sources_cascade_2026_04_27` | field_sources jsonb + helper 함수 |

다음 세션에서 `supabase db pull` 또는 수동 export 권장.

---

## 9. 절대 보존 자료 (재확인)

- `_ARCHIVE_PRESERVE_NOTICE.md`
- 인계 v1, v2, v3, v4 모두 보존
- `outputs/real_estate_listing_report_2026.md` (글로벌 SOTA 조사)
- `_NAVER_SEO_SETUP.md` (네이버 안내)
- `_CRON_SECRET_SETUP.md` (이미 적용됨, 보존만)
- `_PUSH_NOW.bat` (백업, 사용 안 함)

---

## 10. 알려진 워크플로우 이슈 (재현)

- Edit 도구 truncate 버그 (이번 세션 4번 발생) — heredoc 우회
- .git/index.lock permission denied — `/tmp/wishes-push` alternates 우회
- tsc 깨짐 — node 로 syntax check 우회
- Chrome MCP 네이버 사이트 navigate 차단 (안전 제한)

---

## 11. 솔직한 한계

1. **v2 라우트 라이브 검증 미완** — admin 로그인 토큰 필요. 사용자 본인이 admin 페이지에서 직접 확인.
2. **검수 UI 미작성** — v2 호출 버튼이 admin 페이지에 없음. 다음 세션에 추가.
3. **Gemini 무료 한도 일 100K 토큰** — 매물 1건당 ~2K 토큰 가정 시 일 50건. 사용자 정책 (직접 클릭) 부합. 초과 시 Anthropic fallback (월 ~$5).
4. **600건 처리 미결정** — 사용자 옵션 선택 필요.
5. **카카오 directions / OSM Overpass 미적용** — Phase 2 RAG 강화 작업.

---

## 12. 한 줄 요약

**보안 + 파싱 + cron + cascade + SEO + Gemini v2 시스템 = 모두 라이브.**
**다음 세션: v2 검수 UI + 600건 처리 + cascade admin PATCH 보강.**

작성: 2026-04-27 v4 (라이브 commit `4378b6b`, 마라톤 세션 종료)
