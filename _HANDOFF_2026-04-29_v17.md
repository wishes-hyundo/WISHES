# WISHES 다음 세션 인계 (2026-04-29 v17)

이전 v16 의 모든 내용 + 이번 마무리 작업.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## v17 핵심 변경 — DB 자체 정규화 (영구 해결)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

사장님 명령 (2026-04-29):
  "온하우스 수집한 매물 모두 적용 시켜야지 앞으로 등록되는 매물도 계속"

이전엔 v328/v330 등 클라이언트 patch 로 카드 텍스트 후처리 → 캐시·DOM 의존.
v17 부터 **DB 자체 address 컬럼이 풀형식**:

### SQL 함수 + Trigger
- `public.normalize_listing_address(addr, building_name, building_dong, floor_current, building_ho)`
  - 매물 61443 기준 형식: `[지번주소] [건물명] [N층] [동] [호]`
  - 슬래시(/Nt) 제거 / 분모 제거 / "호" 글자 제거 / 쉼표 정리
  - idempotent — 자기 출력 재입력 시 중복 안 생김
  - 풀형식 매물은 그대로 통과 (slash·"호"만 정리)
- BEFORE INSERT/UPDATE trigger `listings_normalize_address`
  - address / building_name / building_dong / floor_current / building_ho 변경 시 자동 호출
  - 신규 매물 / 수정 매물 모두 자동 풀형식

### 일괄 정규화 결과 (한 번 실행)
- **19,518 매물 UPDATE 완료**
- 사장님 화면 12건 매물 모두 61443 기준:
  - 62752 엠코빌 → "서울 관악구 신림동 251-212 엠코빌 3층"
  - 62381 맥스텔 → "서울 관악구 봉천동 866-11 맥스텔 3층"
  - 62427 → "서울 관악구 봉천동 895-14 7층 702" (쉼표 제거)
  - 61441/61442 → "...201" / "...301" ("호" 글자 제거)

### admin minimal cache 무효
- `listings-minimal-v11` → `v12` (사장님 새로고침 시 즉시 반영)

### 클라이언트 patches 위치
- v324~v331 그대로 살려두지만, address 정규화 됐으니 v328 의 일부 work 는 무용:
  - DB 가 (괄호) 이미 제거 → patch 가 stripped 통과
  - DB 가 풀형식 → patch 의 buildText 풀형식 분기 통과
  - patch 는 "이미 정규화된 텍스트" 받아 안전 통과
- 다음 세션엔 v328 patch 단순화 또는 제거 검토 가능

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 사장님 작업 (1번만)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔗 https://wishes.co.kr/admin/onhouse-setup
- ID/PW 입력 → 저장 클릭 → onhouse 로그인 테스트 자동 → DB 저장
- 등록 즉시 매일 03:00 KST cron 으로 호수 자동 enrich
- "호수 추출 테스트" 버튼으로 즉시 1건 sample 검증 가능
- ID/PW 는 Supabase app_secrets (RLS service-role only) 에 저장

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 활성 자동화 (모두 사장님 손 X)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- DB trigger `listings_normalize_address` — 매물 변경 시 즉시 정규화
- Vercel cron 매 30분: `/api/admin/enrich-roadname?mode=roadname&batchSize=80`
  → 16,500건 도로명 약 4일 내 100% 완료
- Vercel cron 매일 03:00 KST: `/api/admin/enrich-onhouse-detail?batchSize=20`
  → ID/PW 등록 후 자동 시작 (등록 전엔 graceful skip)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 다음 세션 즉시 액션
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. /tmp/work-YYYY-MM-DD/wishes-clone fresh clone
2. live audit 8/8 PASS 확인
3. 도로명 백필 진행률:
```sql
SELECT COUNT(*) FILTER (WHERE building_info->>'도로명주소' IS NOT NULL AND building_info->>'도로명주소' != '') AS done,
       COUNT(*) AS total FROM listings;
```
4. onhouse 호수 enrich 결과:
```sql
SELECT COUNT(*) FROM listings WHERE source_site='onhouse' AND building_ho IS NOT NULL;
```
5. address 정규화 idempotent 검증:
```sql
SELECT COUNT(*) FROM listings WHERE address ~ '(\d+)\s*층\s+\1\s*층';  -- 0 이어야
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 이번 세션 commits 마지막
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 5d66081 onhouse-setup 페이지
- b720aa7 인계 v16
- 9845d7d v331 + img-proxy + cron
- ad739a0 v328b/v330
- d2c23d5 v327b/v328/v329
- 36f396c list API building_info
- 840d8ec enrich-roadname
- 3957751 v326
- ecee7bd v325
- de7db41 v324b
- e9bdd17 v324
이번 세션 종료: 2026-04-29 KST
