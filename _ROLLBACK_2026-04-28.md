# /search 본가 swap 롤백 (2026-04-28)

## 사용자 피드백
> "기존 UI 가 훨씬 좋고 기능 너무 많이 빠짐. 성능을 올리라고 한 거지 디자인을 바꾸라는 게 아닌데"

## 잘못 이해한 것
- ❌ Claude 가 한 것: UI 디자인까지 BoB 로 새로 만들고 옛날 코드 삭제
- ✅ 사장님이 원한 것: **옛날 UI/기능 그대로**, **내부 코드만** 2026 BoB 기술로 성능 개선

## 즉시 조치
- main 을 099d4cb 로 reset (옛날 가게 + 미사용 BoB 컴포넌트 보존)
- public/search/ 18개 파일 (22,908줄) 모두 복원
- src/app/search/page.tsx 옛날 451줄 복원
- 라이브 검증

## BoB 컴포넌트 (10개, 1,725줄)
모두 src/components/bob/ + src/components/ui/ 에 보존.
다음 세션에 옛날 UI 그대로 두고 **내부 성능만** 개선하는 방식으로 활용 예정:
- 매물 fetch 캐싱 (TanStack Query)
- 이미지 lazy load 개선
- IndexedDB 활용
- Service Worker 최적화

## 옛날 UI/기능 100% 보존
- /search → 옛날 가게 (content.js + 14 patch)
- /admin/* → 옛날 그대로
