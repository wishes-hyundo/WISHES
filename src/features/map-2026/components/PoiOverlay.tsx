// L-naver-2026minimal1 (2026-04-27): 사용자 요청으로 학세권 POI 기능 제거.
//   파일은 빈 default export 로 유지 (history). 향후 필요 시 git 에서 복원 가능.
export type PoiCategoryKey = never;
export const POI_CATEGORY_LIST: never[] = [];
export default function PoiOverlay() { return null; }
